var dns = require ('dns')

var hosts = [ "nytimes.com", "google.com" ];
var ips = [], errs = [];
await {
      for (var i in hosts) {
	  dns.resolve (hosts[i], defer (errs[this.i], ips[this.i]));
      }
}
for (var i in hosts) {
     console.log (hosts[i] + " -> " + ips[i]);
}
