
var slots = {};
var n = 40;
var assert = require ("assert");


function run () {
	var i = 0;	
	while (i < n) {
	    await { 
		    setTimeout (defer (), 100); 
		    setTimeout (defer (), 2); 
	    }
        console.log ("foo");
	    i++;
	}
}

exports.run = run;
